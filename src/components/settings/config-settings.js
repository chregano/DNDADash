import { defaultFont } from "src/theme/core/typography";

// ----------------------------------------------------------------------

export const STORAGE_KEY = "app-settings-2";

export const defaultSettings = {
  colorScheme: "dark",
  direction: "ltr",
  contrast: "default",
  navLayout: "vertical",
  primaryColor: "blue",
  navColor: "integrate",
  compactLayout: true,
  fontFamily: defaultFont,
};
